# Base URL for the FastAPI backend.
# In local dev, Vite proxies /api to this automatically (see vite.config.js),
# so this is mainly used for production builds or overriding the proxy.
VITE_API_BASE_URL=http://localhost:8000/api
